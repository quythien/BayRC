% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/permutation_sim.R
\name{analyze_pathway_concordance}
\alias{analyze_pathway_concordance}
\title{Analyse concordance for a single pathway with permutation test}
\usage{
analyze_pathway_concordance(
  rho_A,
  rho_B,
  idx = NULL,
  n_perm = 1000,
  n_boot = 500,
  alpha = 0.05,
  use_cpp = FALSE,
  is_global = FALSE,
  compute_pvalue = TRUE,
  compute_ci = TRUE
)
}
\arguments{
\item{rho_A}{G x K binary matrix; posterior rho samples for condition A.}

\item{rho_B}{G x K binary matrix; posterior rho samples for condition B.}

\item{idx}{Integer vector or \code{NULL}; row indices of pathway genes;
if \code{NULL}, use all genes.}

\item{n_perm}{Integer; permutations for the p-value (default 1000).}

\item{n_boot}{Integer; bootstrap replicates for the CI (default 500).}

\item{alpha}{Numeric; significance level (default 0.05).}

\item{use_cpp}{Logical; use Rcpp implementation if available (default
\code{FALSE}).}

\item{is_global}{Logical; skip pathway subsetting (default \code{FALSE}).}

\item{compute_pvalue}{Logical; compute permutation p-value (default
\code{TRUE}).}

\item{compute_ci}{Logical; compute bootstrap CI (default \code{TRUE}).}
}
\value{
A list with elements \code{mean_adj_jaccard} (observed),
  \code{p_value}, \code{ci_lower}, \code{ci_upper}, and related
  diagnostic fields.
}
\description{
Computes the observed adjusted Jaccard concordance for a single pathway
(optionally subsetting to pathway gene indices), then tests significance
via gene-label permutation and optionally computes bootstrap confidence
intervals.
}
