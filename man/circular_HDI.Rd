% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimates.R
\name{circular_HDI}
\alias{circular_HDI}
\title{Compute the shortest circular highest-density interval}
\usage{
circular_HDI(samples, credMass = 0.95, P = 24, a = 0)
}
\arguments{
\item{samples}{Numeric vector; phase samples in hours.}

\item{credMass}{Numeric; desired coverage probability (default 0.95).}

\item{P}{Numeric; period / circle circumference in hours (default 24).}

\item{a}{Numeric; left endpoint of the normalisation interval (default 0).}
}
\value{
List with elements \code{lower} and \code{upper} (both in
  \[a, a + P)) giving the bounds of the shortest HDI.
}
\description{
Finds the shortest interval on a circle of circumference \code{P} that
contains at least \code{credMass} of the posterior samples.  Uses a
sliding-window algorithm on a doubled sorted array to handle wrap-around.
}
