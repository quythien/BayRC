% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcmc_time.R
\name{CBt_MCMC_single}
\alias{CBt_MCMC_single}
\title{CBt MCMC single chain with time-error model}
\usage{
CBt_MCMC_single(
  Data.list,
  Init.value,
  P = 24,
  iteration = 1000,
  thin = 20,
  n.burn = 1000,
  seed = 15213,
  p_rhythmic = rep(0.2, 1000),
  rj.p.stay = 0.5,
  t_p_gene = "rhythmic",
  fix_rhythm = c(rep(1, 10), rep(0, 990)),
  p_rhythm_cut = 0.7,
  t_p_prior = "VM",
  MH_kappa_t_p = 0.5,
  theta_t_p,
  kappa_t_p,
  kappa_t0_p,
  alpha_t1,
  alpha_t2,
  A_prior = "Jeffreys_OLS_condi",
  mu_A = 1,
  sigma_A = 10^2,
  A.min = 0,
  A_wb_beta2 = 2,
  A_gm_shape = 1.99,
  A_gm_rate = 0.5,
  rj.phi = TRUE,
  rj.A = TRUE,
  mu_M = 0,
  sigma_M = 10^2,
  sigma_prior_v = 4,
  sigma_prior_s = 1,
  save.file = "MCMC_save.rds",
  save.file2 = "MCMC_save.rds",
  save.file3 = "MCMC_save.rds"
)
}
\arguments{
\item{Data.list}{A named list with three elements: \code{data} (G x N
data.frame), \code{time} (length-N Zeitgeber time in hours), and
\code{gname} (length-G gene identifiers).}

\item{Init.value}{List returned by \code{CBt_init_single} containing
starting values for \code{rho}, \code{M}, \code{A}, \code{phi},
\code{sigma}, and \code{t_p}.}

\item{P}{Numeric; period in hours (default 24).}

\item{iteration}{Integer; total iterations including burn-in (default 1000).}

\item{thin}{Integer; thinning interval (default 20).}

\item{n.burn}{Integer; burn-in iterations (default 1000).}

\item{seed}{Integer; random seed (default 15213).}

\item{p_rhythmic}{Numeric vector length G; prior Pr(rho = 1) per gene.}

\item{rj.p.stay}{Numeric; probability of skipping between-model move.}

\item{t_p_gene}{Character; which genes inform the t_p update: one of
\code{"rhythmic"}, \code{"fixed"}, or \code{"p_rhythmic"}.}

\item{fix_rhythm}{Length-G binary vector used when
\code{t_p_gene = "fixed"}.}

\item{p_rhythm_cut}{Numeric threshold for \code{t_p_gene = "p_rhythmic"}.}

\item{t_p_prior}{Character; prior on t_p: \code{"VM"} (von Mises),
\code{"mass0_spike"}, \code{"cont_spike"}, or \code{"cnot0_spike"}.}

\item{MH_kappa_t_p}{Numeric; Metropolis-Hastings step size for t_p.}

\item{theta_t_p, kappa_t_p, kappa_t0_p, alpha_t1, alpha_t2}{Numeric;
hyperparameters for the t_p prior.}

\item{A_prior}{Character; amplitude prior name (see
\code{CB_MCMC_single_rj_slice}).}

\item{mu_A, sigma_A, A.min}{Numeric; truncated-Normal amplitude prior.}

\item{A_wb_beta2, A_gm_shape, A_gm_rate}{Numeric; alternative amplitude
prior parameters.}

\item{rj.phi, rj.A}{Logical; jointly propose phi/A in RJMCMC jump.}

\item{mu_M, sigma_M}{Numeric; MESOR prior mean and variance.}

\item{sigma_prior_v, sigma_prior_s}{Numeric; inverse-gamma prior on
residual variance.}

\item{save.file, save.file2}{Character; paths for intermediate saves.}
}
\value{
Same structure as \code{CB_MCMC_single_rj_slice} plus a
  \code{t_p} matrix (N x K) of posterior samples of time-error.
}
\description{
Extends \code{CB_MCMC_single_rj_slice} to jointly infer the gene
expression parameters and the latent Zeitgeber-time measurement error
(t_p) for each sample.  The time-error t_p is modelled with a
von Mises or spike-and-slab prior and updated via Metropolis-Hastings
within each iteration.
}
\details{
Run MCMC for a single circadian dataset with Zeitgeber-time uncertainty
}
\examples{
\dontrun{
sim  <- CBt_sim_data()
dat  <- list(data  = as.data.frame(sim$data[[1]]$dat),
             time  = sim$data[[1]]$x$time,
             gname = paste0("G", seq_len(nrow(sim$data[[1]]$dat))))
init <- CBt_init_single(dat)
mcmc_out <- CBt_MCMC_single(dat, init,
              iteration = 200, thin = 10, n.burn = 100,
              theta_t_p = 0, kappa_t_p = 0.5, kappa_t0_p = 0.1,
              alpha_t1 = 1, alpha_t2 = 1)
}
}
