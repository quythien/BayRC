% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_heatmap.R
\name{plot_heatmap}
\alias{plot_heatmap}
\title{Pathway-level expression heatmap}
\usage{
plot_heatmap(
  data1,
  data2,
  pathway_genes,
  pathway_name,
  phase_results,
  transition_results = NULL,
  group_names = c("Group1", "Group2"),
  save_path = NULL,
  n_bins = 24,
  versions = c("full", "rhythmic_only", "both")
)
}
\arguments{
\item{data1}{Named list; MCMC output for condition 1 with \code{rho}
and \code{phi} matrices.}

\item{data2}{Named list; MCMC output for condition 2.}

\item{pathway_genes}{Character vector; gene names in the pathway.}

\item{pathway_name}{Character; pathway label used in the plot title.}

\item{phase_results}{Output from \code{phase_infer} or a similar list
with per-gene phase metrics.}

\item{transition_results}{Output from \code{transition_classify} or
\code{NULL} (default \code{NULL}); used to colour genes by
gain/loss/maintained status.}

\item{group_names}{Character vector of length 2; condition labels
(default \code{c("Group1", "Group2")}).}

\item{save_path}{Character or \code{NULL}; file path for saving the
plot PNG; if \code{NULL} the plot is drawn but not saved.}

\item{n_bins}{Integer; number of bins for the phase colour wheel
(default 24).}

\item{versions}{Character; which version to produce: \code{"full"},
\code{"rhythmic_only"}, or \code{"both"} (default \code{"full"}).}
}
\value{
Called for side effects; invisibly returns the heatmap object.
}
\description{
Produces a \code{ComplexHeatmap} visualisation for a given pathway,
showing posterior rhythmicity probabilities and phase information for
both conditions side by side.  Genes can be coloured by gain/loss/
maintained status from \code{transition_classify} output.  The plot
can show all genes, only rhythmic genes, or both (two panels).
}
\details{
Integrated pathway heatmap with rhythmicity and phase information
}
