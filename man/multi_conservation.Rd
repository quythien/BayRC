% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/permutation_sim.R
\name{multi_conservation}
\alias{multi_conservation}
\title{Multi-dataset pairwise pathway concordance analysis}
\usage{
multi_conservation(
  mcmc.merge.list,
  dataset.names,
  select.pathway.list,
  n_perm = 1000,
  n_boot = 500,
  alpha = 0.05,
  min.p = 5,
  qvalue_threshold = NULL,
  output.dir = "Conservation_Pathway_Bootstrap",
  save_output = TRUE,
  use_cpp = FALSE,
  compute_pvalue = TRUE,
  compute_ci = TRUE
)
}
\arguments{
\item{mcmc.merge.list}{Named list of MCMC output lists.}

\item{dataset.names}{Character vector; condition labels.}

\item{select.pathway.list}{Named list of pathway gene sets.}

\item{n_perm}{Integer; permutations per pathway (default 1000).}

\item{n_boot}{Integer; bootstrap replicates per pathway (default 500).}

\item{alpha}{Numeric; significance level (default 0.05).}

\item{min.p}{Integer; minimum number of pathway genes required to test
a pathway (default 5).}

\item{qvalue_threshold}{Numeric or \code{NULL}; q-value significance
cutoff.}

\item{output.dir}{Character; directory for Excel output.}

\item{save_output}{Logical; if \code{TRUE} (default), write results to an
Excel file in \code{output.dir}. Set \code{FALSE} to skip the write
(e.g. in tests or CI).}

\item{use_cpp}{Logical; if \code{TRUE}, use the Rcpp-accelerated
concordance computation (default \code{FALSE}, pure R).}

\item{compute_pvalue}{Logical; if \code{TRUE} (default), run the
permutation test for statistical significance.}

\item{compute_ci}{Logical; if \code{TRUE} (default), run bootstrap
resampling for a 95\% confidence interval on the adjusted concordance.}
}
\value{
Named list of pairwise result data.frames.  Also writes an
  Excel file to \code{output.dir}.
}
\description{
Runs all pairwise pathway bootstrap concordance analyses across a list of
MCMC outputs and compiles results into Excel output.  Wraps
\code{bootstrap_conservation_pathway_within}.
}
