% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimates.R
\name{get_t_phi_CI_est}
\alias{get_t_phi_CI_est}
\title{Compute circular estimate and HDI for phase or peak-time}
\usage{
get_t_phi_CI_est(
  row_MCMC,
  P = 24,
  credMass = 0.95,
  burn = 1,
  rho = 1,
  t = TRUE,
  a = 0
)
}
\arguments{
\item{row_MCMC}{Numeric vector; posterior samples for one gene.}

\item{P}{Numeric; period (default 24).}

\item{credMass}{Numeric; HDI coverage (default 0.95).}

\item{burn}{Integer; burn-in samples to discard (default 1).}

\item{rho}{Numeric vector; rhythmicity samples (used to gate A samples
when \code{t = FALSE}).  Set to 1 for t_p.}

\item{t}{Logical; if \code{TRUE} compute for peak time; if \code{FALSE}
compute for acrophase phi.}

\item{a}{Numeric; left endpoint of the normalisation interval (default 0).}
}
\value{
Named numeric vector with elements \code{phi.Est},
  \code{phi.Lower}, \code{phi.Upper}.
}
\description{
Returns the circular median and \code{circular_HDI} bounds for a single
gene's phase (phi) or peak-time (t_p) MCMC chain, after discarding
burn-in and optionally filtering by rhythmicity.
}
\keyword{internal}
