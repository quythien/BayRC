% Generated by roxygen2: do not edit by hand
% Please edit documentation in ./<text>
\name{plot_pathway_profiles}
\alias{plot_pathway_profiles}
\title{Paired rhythmicity and phase profiles for a selected pathway}
\usage{
plot_pathway_profiles(
  data,
  pathway_genes,
  tissues,
  concordance,
  pathway_name = "Selected pathway",
  tissue_k = 2L,
  tissue_linkage = "ward.D2",
  gene_k = 3L,
  min_gene_fraction = 0.5,
  min_gene_overlap = 2L,
  min_tissue_overlap = 3L,
  min_groups = 1L,
  period = 24,
  file = NULL,
  draw = TRUE,
  width = 16,
  height = 9,
  show_limited = TRUE,
  sparse_method = "strict",
  shrink_strength = 2
)
}
\arguments{
\item{data}{Phase-summary data as in \code{pathway_phase_hierarchy}, with
an additional posterior column containing rhythmicity probabilities.}

\item{pathway_genes, tissues, pathway_name}{See \code{pathway_phase_hierarchy}.}

\item{concordance}{Named symmetric pathway rhythmic-concordance matrix on
[-1,1]. This must come from rhythmicity analysis, not phase clustering.}

\item{tissue_k}{Number of rhythmic tissue groups (specified in advance).}

\item{tissue_linkage}{Linkage for rhythmic concordance clustering. ward.D2
reproduces the original manuscript convention applied to 1-concordance;
its variance-minimization interpretation requires Euclidean distances.}

\item{gene_k}{Number of gene phase groups.}

\item{min_gene_fraction}{Minimum called fraction per rhythmic tissue block.}

\item{min_gene_overlap, min_tissue_overlap, min_groups}{See
\code{pathway_phase_hierarchy}.}

\item{period}{Cycle length in hours.}

\item{file}{Optional PDF path; NULL draws on the current device.}

\item{draw}{Whether to draw the panels.}

\item{width, height}{PDF dimensions in inches.}

\item{show_limited}{Whether to display genes excluded by the phase-coverage
requirement in a separate limited-coverage block (default TRUE). This block
is not a learned phase group; hiding it does not change clustering.}

\item{sparse_method}{Either strict (original overlap checks) or shrink
(exploratory regularization of sparse distances toward neutral loss 1).}

\item{shrink_strength}{Positive regularization constant. In shrink mode,
a comparison supported by n observations receives weight n/(n+strength).}
}
\value{
Invisibly returns rhythmic tree, learned memberships and group scores,
  selected group, phase hierarchy, plotted matrices and display orders.
}
\description{
Paired rhythmicity and phase profiles for a selected pathway
}
\details{
Learns tissue groups from rhythmic concordance, identifies the group
  with highest mean off-diagonal concordance, then learns gene phase groups
  across all supplied tissues with equal block weights. Within the selected
  rhythmic group, phase clustering orders tissues without changing membership.
  Both panels retain all observed pathway genes; genes failing phase coverage
  are shown after eligible genes. Gray phase cells are not called rhythmic.
  The same gene and tissue order is used in both panels. Selection of the
  most concordant group is explicit and descriptive, not a significance test.
  If coverage or overlap prevents phase clustering, a warning is issued,
  hierarchy is NULL, and phase_status explains why. Both data panels remain
  available in rhythmic tissue order, without assigned gene phase groups.
  Phase-group and coverage labels appear only beside the phase panel.
}
