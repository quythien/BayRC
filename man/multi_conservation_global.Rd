% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/global.R
\name{multi_conservation_global}
\alias{multi_conservation_global}
\title{Multi-condition global circadian conservation analysis}
\usage{
multi_conservation_global(
  mcmc.merge.list,
  dataset.names,
  delta = 3,
  units = "hours",
  B = 1000,
  ncores = NULL,
  parallel = "auto",
  rounds = 1,
  save_intermediate = FALSE,
  intermediate_dir = "intermediate_results",
  output.dir = "Conservation_Global"
)
}
\arguments{
\item{mcmc.merge.list}{Named list of MCMC output lists, one per condition.}

\item{dataset.names}{Character vector; labels for each condition.}

\item{delta}{Numeric; phase-shift threshold (default 3).}

\item{units}{Character; \code{"hours"} (default).}

\item{B}{Integer; permutations per pair (default 1000).}

\item{ncores}{Integer or \code{NULL}; parallel cores.}

\item{parallel}{Logical or \code{"auto"}; parallelisation strategy.}

\item{rounds}{Integer; permutation rounds per pair.}

\item{save_intermediate}{Logical; save intermediate permutation results.}

\item{intermediate_dir}{Character; directory for intermediate saves.}

\item{output.dir}{Character; directory for the Excel output file
(default \code{"Conservation_Global"}).}
}
\value{
Named list of square matrices (one per metric): congruence scores
  and p-values, gain and loss indices, ratio scores and p-values.
  Also writes an Excel file to \code{output.dir}.
}
\description{
Runs all pairwise comparisons among a list of MCMC outputs: for each
pair computes the observed congruence, gain, and loss indices; performs
permutation tests via \code{perm_conservation_global}; applies BH
multiple-testing correction; and writes an Excel summary with one sheet
per metric.
}
\details{
Pairwise global conservation analysis across multiple conditions
}
