% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pathway.R
\name{perm_pathway_circadian}
\alias{perm_pathway_circadian}
\title{Generate permutation null distribution for pathway conservation scores}
\usage{
perm_pathway_circadian(
  dat1,
  dat2,
  select.pathway.list,
  delta = 3,
  units = "hours",
  B = 1000,
  ncores = NULL,
  parallel = "auto",
  rounds = 1,
  save_intermediate = FALSE,
  intermediate_dir = NULL
)
}
\arguments{
\item{dat1, dat2}{Named MCMC output lists with \code{rho} matrices and
gene symbols set via \code{match_symbols}.}

\item{select.pathway.list}{Named list of character vectors; pathway
gene sets.}

\item{delta}{Numeric; phase threshold (default 3).}

\item{units}{Character; \code{"hours"} (default).}

\item{B}{Integer; number of permutations (default 1000).}

\item{ncores}{Integer or \code{NULL}; parallel cores.}

\item{parallel}{Logical or \code{"auto"}; parallelisation strategy.}

\item{rounds}{Integer; rounds to split B permutations (default 1).}

\item{save_intermediate}{Logical; save each round's output to disk.}

\item{intermediate_dir}{Character; directory for intermediate saves.}
}
\value{
A named list of K arrays (one per pathway), each B x 4
  containing permuted congruence, gain, loss, and ratio scores.
}
\description{
Runs gene-label permutations within each pathway to build a null
distribution for the congruence, gain, and loss indices.  Calls
\code{run_single_round} internally and supports parallel execution and
multi-round splits to manage memory.
}
