% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimates.R
\name{recenter_samples}
\alias{recenter_samples}
\title{Recenter phase samples around their circular median}
\usage{
recenter_samples(samples, P = 24, a = 0)
}
\arguments{
\item{samples}{Numeric vector; phase samples in hours.}

\item{P}{Numeric; period (default 24).}

\item{a}{Numeric; left endpoint of normalisation interval (default 0).}
}
\value{
List with \code{shifted} (re-centred samples) and \code{med}
  (the circular median used for re-centring).
}
\description{
Shifts samples so that their circular median is at zero, then wraps
into \[a, a + P).  Used internally before the sliding-window HDI.
}
\keyword{internal}
