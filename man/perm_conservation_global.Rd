% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/global.R
\name{perm_conservation_global}
\alias{perm_conservation_global}
\title{Global permutation test for circadian congruence}
\usage{
perm_conservation_global(
  dat1,
  dat2,
  delta = 3,
  units = "hours",
  B = 1000,
  ncores = NULL,
  parallel = "auto",
  rounds = 1,
  save_intermediate = FALSE,
  intermediate_dir = NULL
)
}
\arguments{
\item{dat1}{Named list; MCMC output for condition 1 with a \code{rho}
matrix (G x K binary).}

\item{dat2}{Named list; MCMC output for condition 2 with a \code{rho}
matrix.}

\item{delta}{Numeric; passed to \code{congruence} (default 3; currently
unused in the probabilistic score).}

\item{units}{Character; \code{"hours"} or \code{"radians"} (default
\code{"hours"}).}

\item{B}{Integer; total number of permutations (default 1000).}

\item{ncores}{Integer or \code{NULL}; number of cores for parallel runs;
\code{NULL} auto-detects (up to 4).}

\item{parallel}{Logical or \code{"auto"}; whether to parallelise
(default \code{"auto"}: enabled on Unix when B >= 100).}

\item{rounds}{Integer; split B permutations into this many rounds to
reduce memory use (default 1).}

\item{save_intermediate}{Logical; save each round to \code{intermediate_dir}
(default \code{FALSE}).}

\item{intermediate_dir}{Character; directory for intermediate saves.}
}
\value{
A B x 4 numeric matrix with columns \code{congruence_index},
  \code{gain_index}, \code{loss_index}, \code{gain_loss_ratio}.
}
\description{
Generates a permutation null distribution for the congruence index and
related metrics (gain, loss, gain/loss ratio) by randomly shuffling the
row order of \code{dat2$rho} while keeping \code{dat1$rho} fixed.
Supports parallelisation via \code{parallel::mclapply} (Unix only) and
multi-round execution to reduce peak memory use.
}
\details{
Permutation null distribution for global congruence index
}
